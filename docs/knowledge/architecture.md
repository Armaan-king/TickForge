# Architecture

The shape of the system, and — more importantly — where its boundaries are and
why. Not a file listing. Not a dependency graph.

## Shape

_TBD: the system in a few sentences. What the major pieces are and what each is
responsible for._

## Boundaries

The rules that must hold. Each one states what breaks if it's violated — a rule
without a consequence gets ignored.

### _TBD: boundary name_

- **Rule:** _TBD — e.g. "X never imports Y"_
- **Why:** _TBD_
- **Breaks if violated:** _TBD_

## Data flow

_TBD: how information moves through the system, in concepts rather than
function calls. The pipeline stages and what each produces._

```
TBD
```

## Deliberately not abstracted

_TBD: places where duplication or coupling is intentional, so nobody "fixes"
it. State the reason._
