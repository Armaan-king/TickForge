# _Concept Name_

Copy this file to start a new concept. Delete any section you have nothing real
to say in — an empty heading is worse than a missing one.

## Purpose

_One or two sentences. What this exists to do._

## Responsibilities

- _TBD_

## Does NOT handle

_The most valuable section. What belongs elsewhere, so nobody drags it in here._

- _TBD_

## Conceptual flow

_How this fits the pipeline, in domain terms — what it consumes and what it
produces. Not module wiring; if this starts naming imports, delete it and grep
instead._

```
TBD ──produces──▶ TBD ──consumed by──▶ TBD
```

## Design reason

_Why this is a separate thing at all, and what future capability the separation
protects. This is the part no grep can recover._

## Gotchas

_Traps specific to this component. Cross-cutting ones go in `pitfalls.md`._

## Related code

_Directories, not files — paths move, directories mostly don't. A hint for
where to start reading, not an index._

- `TBD/`
